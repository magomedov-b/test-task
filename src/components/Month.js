import React from 'react';

const Month = (props) => {
    return (
        <div className="Month">
            <p className="Month-name">{props.name}</p>
        </div>
    );
};

export default Month;